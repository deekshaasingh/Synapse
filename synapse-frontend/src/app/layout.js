import './globals.css';

export const metadata = {
  title: 'Synapse — Infrastructure Simulation',
  description: 'Real-time event-driven infrastructure simulation engine',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
